#!/bin/bash

# Source the standardized environment configuration
SCRIPT_DIR=$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")
source "$SCRIPT_DIR/qp2_env.sh"

# Execute the Python script
# PROJECT_DIR is defined in qp2_env.sh and points to the qp2 package root
$MYPYTHON "$PROJECT_DIR/simulated_collection/mock_redis_streamer.py" "$@"
